<!DOCTYPE html>
<html lang="uz">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Daily plans</title>
  <meta name="description" content="Reja va Maqsadlar — Your personal daily planner." />

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

  <!-- Custom CSS -->
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
    <div class="main-wrapper">
    <div class="text-end">
      <!-- Team nomi DYNAMIC shakilda yozing-->
  👋 Welcome, <strong>Jay</strong> |
      <!-- Team nomi /DYNAMIC shakilda yozing-->
  <a href="/logout" class="btn btn-outline-danger btn-sm">Logout</a>
</div>
    <h1 class="text-center display-5 mb-4 text-primary">📋 DAILY PLANS</h1>

    <!-- Form -->
    <div class="card p-4 mb-4 bg-white">
      <form id="create-form" action="/create-item" method="POST">
        <div class="row g-3">
          <div class="col-12 col-md-9">
            <input
              id="create-field"
              name="reja"
              type="text"
              class="form-control"
              placeholder="Today's plan..."
              autocomplete="off"
              required
            />
          </div>
          <div class="col-12 col-md-3 d-grid">
            <button class="btn btn-primary btn-rounded" type="submit">
              ➕ Add
            </button>
          </div>
        </div>
      </form>
    </div>

    <!-- Item List DYNAMIC -->
    <ul id="item-list" class="list-group mb-4">
      <li class="list-group-item d-flex justify-content-between align-items-center">
        <div class="d-flex align-items-center gap-2">
          <span class="item-text">
            Let's start TEAMWORK!
          </span>
        </div>
        <div class="d-flex gap-2">
          <button data-id="////DYNAMIC////" class="edit-me btn btn-sm btn-outline-secondary">✏️</button>
          <button data-id="////DYNAMIC////" class="delete-me btn btn-sm btn-outline-danger">🗑️</button>
        </div>
      </li>
    </ul>

    <!-- Delete All Button -->
    <div class="d-grid">
      <button id="clean-all" class="btn btn-danger btn-rounded">
        🧹 Clean all
      </button>
    </div>
  </div>

  <!-- Scripts -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/axios/1.9.0/axios.min.js"></script>
  <script src="/browser.js"></script>
</body>
</html>
